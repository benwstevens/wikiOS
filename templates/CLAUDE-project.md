# Instructions for Claude (project wiki)

<!-- Template: copy to each project's .canvas/CLAUDE.md. -->

Read `.canvas/schema.md` first — it is the operating contract for this project's wiki, and Step 0 (the freshness check against the install's canonical `project-schema.md`) comes before any work. You own `01 - Overview/wiki.md`; everything else in the numbered folders is read-only raw source. Never ingest your own output (the wiki, `Claude Session` logs) as a source. Tier 1 additions commit directly; Tier 2 changes are asked or flagged, never silent (wiki-os §6). Snapshot before editing. Log every session (wiki-os §4).
