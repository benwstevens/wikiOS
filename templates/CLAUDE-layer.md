# Start here — <Layer name>

<!-- Template: one per layer, at the layer's root. Short. The root CLAUDE.md above it already imported LLM-rules.md. -->

Read `wiki.md` first. It is the always-on hub for this layer: what it covers and a map to the spoke wikis (each spoke folder's `wiki.md`) that own their domains. The rules for maintaining this layer live in `.system/customization/<layer>-rules.md` (workspace-root path); the cross-cutting rules in `.system/wiki-os.md`. Read both before proposing any edit.

**Privacy:** <state whether this layer is private or shared, what must never be copied out of it, and which other layers it may read.>

**Working conventions.** Big or multi-step job: wiki-os §3 (plan.md, plain-language workflow.md, verify-and-correct loop). End of session: wiki-os §4 (session log into the nearest `Logs/`). To integrate anything and lint: **"update the wikis"** (§8).

## Folder map — where things live (snapshot <YYYY-MM-DD>)
<!-- Optional. A routing aid for low-context requests: folders only, one line each, archives named but not walked. Refresh in place when the tree changes. -->

**Shorthand.** <!-- Grows one line at a time whenever a session has to ask which thing the owner meant. -->
