# <Owner>'s personal preferences (apply in every project on this machine)

<!-- Optional. Copy to ~/.claude/CLAUDE.md (or your tool's global config) ONLY if you want the behaviour rules outside the wiki workspace too. The canonical copy is the workspace's .system/LLM-rules.md; this file is a pointer plus the few rules worth having everywhere. If the two disagree, the workspace file wins; fix this one. -->

> **Canonical copy:** `<workspace root>/.system/LLM-rules.md`, imported by the workspace root `CLAUDE.md`. This file exists for projects outside that workspace.

## Chat responses — recap all requests at the end
When a response contains questions or requests for me (decisions, confirmations, choices), raise them inline where the context lives, and also end the response with a section headed **Requests** (or **Questions**) listing each one as a short bullet, phrased so I can answer them in order without rereading. No section when the response asks nothing.

## Writing in my voice
- No em-dashes in anything drafted under my name. Commas, colons, parentheses, or separate sentences.
- Keep drafts short and plain; the people I write to often read on phones.
- Before drafting anything under my name, read `<workspace root>/.system/customization/writing-style.md`.
